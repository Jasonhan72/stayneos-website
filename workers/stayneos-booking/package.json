{
  "name": "stayneos-booking",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy:staging": "wrangler deploy",
    "deploy:production": "wrangler deploy --name stayneos-booking",
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "wrangler": "^4.78.0"
  }
}
